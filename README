# install dependencies
apt-get install python-setuptools
easy_install pip
pip install virtualenv

# to install dependencies
python setup.py install --user

# to run tests and generate JUnit XML file
nosetests --with-xunit
